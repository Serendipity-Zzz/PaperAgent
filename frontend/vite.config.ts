import react from "@vitejs/plugin-react";
import { defineConfig } from "vitest/config";

export default defineConfig({
  plugins: [react()],
  server: {
    host: "127.0.0.1",
    port: 5173,
    strictPort: true,
    proxy: {
      "/api": {
        target: "http://127.0.0.1:8765",
        changeOrigin: false,
      },
    },
  },
  build: {
    outDir: "dist",
    sourcemap: true,
  },
  test: {
    globals: true,
    environment: "jsdom",
    // Markdown/PDF preview chunks are intentionally large. On Windows, running
    // every jsdom suite in parallel can starve lazy imports and turn passing
    // interaction tests into hook timeouts, so keep the gate deterministic.
    maxWorkers: 1,
  },
});
