---
name: nature-figure-seedream
description: Route non-data scientific illustrations through Seedream while retaining Nature Figure planning and QA contracts.
---

1. Read the approved, pinned upstream `nature-figure` contract and directly required references.
2. Preserve evidence hierarchy, panel map, attribution, typography, legend and QA requirements.
3. Use only the configured Seedream image provider adapter; never rewrite or patch the upstream checkout.
4. Never use a generated image as experimental data or a real-event record.
5. Record provider, base URL, model, prompt, input asset hashes, output hash and user edits.
6. Image API network access and cost require the unified approval flow.
